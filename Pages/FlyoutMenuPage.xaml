<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiTutoriales.Pages.FlyoutMenuPage"
             xmlns:local="clr-namespace:MauiTutoriales.Pages"
             xmlns:local1="clr-namespace:MauiTutoriales.Data"
             Title="FlyoutMenuPage"
             Padding="0,40,0,0"
             IconImageSource="hanburguer.png">
    <CollectionView x:Name="collectionView"
                    x:FieldModifier="public"
                    SelectionMode="Single">
        <CollectionView.ItemsSource>
            <x:Array Type="{x:Type local1:FlyoutPageItem}">
                <local1:FlyoutPageItem Title="Contactos"
                                       IconSource="contacto.png"
                                       TargetType="{x:Type local:Pagina1}"/>
                <local1:FlyoutPageItem Title="Lista de Tareas"
                                       IconSource="listadetareas.png"
                                       TargetType="{x:Type local:Pagina2}"/>
                <local1:FlyoutPageItem Title="Recordatorios"
                                       IconSource="recordatorios.png"
                                       TargetType="{x:Type local:Pagina1}"/>
            </x:Array>
        </CollectionView.ItemsSource>

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Padding="5,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding IconSource}"/>
                    <Label Grid.Column="1"
                           Margin="20,1"
                           Text="{Binding Title}"
                           FontSize="20"
                           FontAttributes="Bold"
                           VerticalOptions="Center"/>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>